## Database Extension

Use MCP tools instead of CLI:

| Instead of...                   | Use                |
|---------------------------------|--------------------|
| `vendor/bin/framework command`  | `framework-action` |

### Benefits

- Token-optimized output
- Structured data format

### Output Modes

`default`, `summary`, `detailed`
